# Targets

Executable drills require a confirmed `TargetProtocolSpec`. Protocol-name-only resolver output may support scope review, but cannot execute drills until authorized scope and in-scope contracts are confirmed.
